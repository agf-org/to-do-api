name: CI

on:
  push:
    branches: [ feature/TODO-52, develop, master ]
  pull_request:
    branches: [ feature/TODO-52, develop, master ]

jobs:
  build_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo Hello, world!
      - run: |
          echo Add other actions to build,
          echo test, and deploy your project.
